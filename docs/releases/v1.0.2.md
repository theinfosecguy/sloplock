# SlopLock v1.0.2

This patch release refreshes generated Action artifacts for the current
multi-ecosystem scanner and keeps the moving `v1` Action ref aligned with the
published npm package.

## Release Integrity

- Keeps `theinfosecguy/sloplock@v1` pointed at `v1.0.2`.
- Publishes `sloplock@1.0.2` to npm with provenance enabled.
- Verifies generated `dist/` artifacts for the bundled JavaScript Action.

## Notes

The Action install ref remains:

```yaml
- uses: theinfosecguy/sloplock@v1
```
